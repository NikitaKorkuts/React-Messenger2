export type InputPropsType = {
    type?: string
    name: string
    checked?: boolean | null
    hasError?: boolean | string
    error?: string
    props?: object
}

export type ExtraPropsType = {
    checked?: true
}